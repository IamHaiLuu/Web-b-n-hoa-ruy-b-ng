{
    "version": 2,
    "buildCommand": "npm run build",
    "builds": [{
        "src": "index.js",
        "use": "@vercel/node",
        "config": {
            "includeFiles": [
                "views/**",
                "public/**"
            ]
        }
    }],
    "routes": [{
        "src": "/(.*)",
        "dest": "/index.js"
    }]
}
